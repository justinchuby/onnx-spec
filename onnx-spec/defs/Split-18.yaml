attributes:
- default_value: 0
  description: Which axis to split on. A negative value means counting dimensions
    from the back. Accepted range is [-rank, rank-1] where r = rank(input).
  name: axis
  required: false
  type: AttrType.INT
- default_value: null
  description: Number of outputs to split parts of the tensor into. If the tensor
    is not evenly splittable the last chunk will be smaller.
  name: num_outputs
  required: false
  type: AttrType.INT
deprecated: false
doc: 'Split a tensor into a list of tensors, along the specified ''axis''.

  Either input ''split'' or the attribute ''num_outputs'' should be specified, but
  not both.

  If the attribute ''num_outputs'' is specified, then the tensor is split into equal
  sized parts.

  If the tensor is not evenly splittable into `num_outputs`, the last chunk will be
  smaller.

  If the input ''split'' is specified, it indicates the sizes of each output in the
  split.

  '
domain: ''
inputs:
- description: The tensor to split
  min_arity: 1
  name: input
  tags:
  - differentiable
  type_str: T
- description: Optional length of each output. Values should be >= 0.Sum of the values
    must be equal to the dim value at 'axis' specified.
  min_arity: 1
  name: split
  tags:
  - optional
  - non-differentiable
  type_str: tensor(int64)
max_input: 2
max_output: 2147483647
min_input: 1
min_output: 1
name: Split
outputs:
- description: One or more outputs forming list of tensors after splitting
  min_arity: 1
  name: outputs
  tags:
  - variadic
  - differentiable
  type_str: T
since_version: 18
support_level: SupportType.COMMON
type_constraints:
- allowed_type_strs:
  - tensor(uint8)
  - tensor(uint16)
  - tensor(uint32)
  - tensor(uint64)
  - tensor(int8)
  - tensor(int16)
  - tensor(int32)
  - tensor(int64)
  - tensor(bfloat16)
  - tensor(float16)
  - tensor(float)
  - tensor(double)
  - tensor(string)
  - tensor(bool)
  - tensor(complex64)
  - tensor(complex128)
  description: Constrain input and output types to all tensor types.
  type_param_str: T
