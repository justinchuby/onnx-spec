attributes:
- default_value: null
  description: ROI pool output shape (height, width).
  name: pooled_shape
  required: true
  type: AttrType.INTS
- default_value: 1.0
  description: Multiplicative spatial scale factor to translate ROI coordinates from
    their input scale to the scale used when pooling.
  name: spatial_scale
  required: false
  type: AttrType.FLOAT
deprecated: false
doc: "\n ROI max pool consumes an input tensor X and region of interests (RoIs) to\n\
  \ apply max pooling across each RoI, to produce output 4-D tensor of shape\n (num_rois,\
  \ channels, pooled_shape[0], pooled_shape[1])."
domain: ''
inputs:
- description: Input data tensor from the previous operator; dimensions for image
    case are (N x C x H x W), where N is the batch size, C is the number of channels,
    and H and W are the height and the width of the data.
  min_arity: 1
  name: X
  tags:
  - differentiable
  type_str: T
- description: RoIs (Regions of Interest) to pool over. Should be a 2-D tensor of
    shape (num_rois, 5) given as [[batch_id, x1, y1, x2, y2], ...].
  min_arity: 1
  name: rois
  tags:
  - non-differentiable
  type_str: T
max_input: 2
max_output: 1
min_input: 2
min_output: 1
name: MaxRoiPool
outputs:
- description: RoI pooled output 4-D tensor of shape (num_rois, channels, pooled_shape[0],
    pooled_shape[1]).
  min_arity: 1
  name: Y
  tags:
  - differentiable
  type_str: T
since_version: 1
support_level: SupportType.COMMON
type_constraints:
- allowed_type_strs:
  - tensor(float16)
  - tensor(float)
  - tensor(double)
  description: Constrain input and output types to float tensors.
  type_param_str: T
